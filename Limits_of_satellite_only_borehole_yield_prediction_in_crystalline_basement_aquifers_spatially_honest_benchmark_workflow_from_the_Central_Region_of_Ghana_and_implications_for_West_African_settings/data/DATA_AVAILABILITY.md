# Data Availability Notes

The GitHub snapshot omits the following local-only tables from version control:

- `Boreholes.csv`

The GitHub snapshot also omits the following raw support tables because they are too large for reliable GitHub hosting:

- `SMAP_SOIL_MOISTURE.csv`
- `MODIS_LAND_COVER_GHANA.csv`

These files are kept only in the local archived workspace copy and are not part of the tracked publication package.

The remaining files in this `data/` folder are the GitHub-sized inputs retained with the repository snapshot.
